services:
  web:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      # Mount the local model/ directory so pre-trained artifacts are
      # available inside the container without rebuilding the image.
      - ./model:/app/model:ro
    restart: unless-stopped
